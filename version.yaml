# Single source of truth for the project version.
#
# Edit this file ONLY. Everything else derives from it:
#   - pyproject.toml        hatchling reads it -> package metadata -> her.__version__
#   - src/her/__init__.py   reads it directly when running from source
#   - desktop/setup_app.py  CFBundle version strings (at build time)
#   - scripts/build-dmg.sh  DMG filename (at build time)
# README.md is intentionally version-agnostic (links point at /releases/latest),
# so it needs no sync.
version: 0.8.0
